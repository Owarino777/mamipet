import type { Metadata } from "next";
import { OwnerAnimalsPage } from "@/interface/app/connected-pages";

export const metadata: Metadata = {
  title: "Mes animaux | MamiPet",
};

export default OwnerAnimalsPage;
